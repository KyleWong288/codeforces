#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <utility>
using namespace std;
typedef long long ll;

// 
void solution(int n) {
    return;
}

int main() {
    ios_base::sync_with_stdio(false);
    int T, n;
    cin >> T;
    while (T--) {
        cin >> n;
        solution(n);
    }
    return 0;
}